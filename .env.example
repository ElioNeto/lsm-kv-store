# LSM-KV-Store Server Configuration
# Copy this file to .env and adjust values as needed

# Server Settings
HOST=0.0.0.0
PORT=8080

# Data Storage
DATA_DIR=./.lsm_data

# Memory Configuration (in bytes)
# Default: 4MB (4 * 1024 * 1024)
MEMTABLE_MAX_SIZE=4194304

# Payload Limits (in bytes)
# Default: 50MB for stress testing and large datasets
# Set to 2097152 (2MB) for production if needed
MAX_JSON_PAYLOAD_SIZE=52428800
MAX_RAW_PAYLOAD_SIZE=52428800

# Storage Configuration
# Block size for SSTable blocks (in bytes)
BLOCK_SIZE=4096

# Block cache size (in MB)
BLOCK_CACHE_SIZE_MB=64

# Sparse index: how many blocks between index entries
SPARSE_INDEX_INTERVAL=16

# Bloom filter false positive rate (0.0 to 1.0)
# Lower = more memory, fewer false positives
BLOOM_FALSE_POSITIVE_RATE=0.01

# Feature Flags Cache
# Time-to-live for feature flags cache (in seconds)
FEATURE_CACHE_TTL=10
