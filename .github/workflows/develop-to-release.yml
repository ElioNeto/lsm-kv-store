- name: Create or update PR to main
  env:
    GH_TOKEN: ${{ github.token }}
    NEW_VERSION: ${{ steps.version.outputs.new_version }}
    BUMP_TYPE: ${{ steps.version.outputs.bump_type }}
  run: |
    RELEASE_BRANCH="release/v${NEW_VERSION}"
    
    if [ "${{ steps.check-pr.outputs.pr_exists }}" -eq 0 ]; then
      gh pr create \
        --base main \
        --head "$RELEASE_BRANCH" \
        --title "Release v${NEW_VERSION}" \
        --body "## 🚀 Release v${NEW_VERSION}
        
**Bump type:** ${BUMP_TYPE}

### ⚙️ Release Configuration

**Escolha o tipo de release editando abaixo:**

Release Type: [lts]

_Opções válidas:_
- \`alpha\` - Release alpha (instável, desenvolvimento)
- \`beta\` - Release beta (pré-release, testes)  
- \`lts\` - Release estável de longo prazo

**Instruções:** Substitua \`lts\` acima por \`alpha\`, \`beta\` ou mantenha \`lts\`

### 📝 Changelog

$(git log --pretty=format:'- %s (%h)' origin/main..HEAD | head -20)

---

_PR gerado automaticamente pelo workflow develop-to-release_" \
        --draft
      
      echo "✅ PR criado: release/v${NEW_VERSION} → main"
    fi
